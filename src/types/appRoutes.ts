export enum EAppRoutes {
    // NOTE: should be in sync with src/app folder structure
    Root = '/',
    Accounts = '/accounts',
    TransactionCategories = '/transaction-categories',
    Settings = '/settings',
}
